# Page-Scroll-Progress-Indicator

A lightweight progress-indicator bar that tracks page scroll depth using vanilla HTML, CSS and JavaScript.

---

## 🧩 Overview  
This project adds a horizontal progress bar at the top of the page that fills in as the user scrolls through the content.  
Built with no external dependencies — pure HTML, CSS & JS.

---

## 🚀 Features  
- Real-time scroll-progress feedback to the user  
- Minimal and clean styling  
- Responsive: works on mobile, tablet & desktop  
- Easy to integrate: drop files into your site and include the script  
- No build step required

---

## 🔧 Customisation Tips

- Change the color and height of the bar by editing the CSS custom properties.
- Enable “stick to top” behavior by giving the bar position: fixed; top: 0;.
- Add smooth transition effects when the bar progresses more gradually.
- Extend script logic: hide the bar on small pages, animate fade-out when complete, etc.

---

## 📌 Why Use This

If you want an elegant, plug-and-play way to provide scroll-depth feedback that doesn’t rely on heavy libraries or frameworks, this is a solid pick. Perfect for blogs, long-form articles, portfolios, documentation pages.
